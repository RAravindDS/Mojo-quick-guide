## we can import anything that exists in python 

from python import Python 


fn main() raises: #Raises is used the function which potentially raise error 
    ## Let's import numpy 
    let np = Python.import_module("numpy") 
    let arr = np.array( [1, 2, 3, 4] )
    print(arr * 2)


